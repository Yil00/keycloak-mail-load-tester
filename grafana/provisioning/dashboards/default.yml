# Provisioning des dashboards (dossier dashboards/)
apiVersion: 1
providers:
  - name: default
    orgId: 1
    folder: Keycloak
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/json
